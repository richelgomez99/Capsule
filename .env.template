# Required — Google Gemini API key for classification, chat, and reflection
GEMINI_API_KEY=your_gemini_api_key

# Optional — Database and upload paths (defaults shown)
# DATABASE_PATH=data/capsule.db
# UPLOAD_DIR=data/uploads
